name: Pull Request

on:
    pull_request:
        types: [opened, synchronize, reopened, edited]

permissions:
    contents: read

jobs:
    pull-request:
        name: Pull Request
        uses: NixySoftware/.github/.github/workflows/pull-request.yml@6b03162f358358a3a6e814434ada3a73997714a9 # v0.6.0
