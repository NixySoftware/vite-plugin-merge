name: Pull Request

on:
    pull_request:
        types: [opened, synchronize, reopened, edited]

permissions:
    contents: read

jobs:
    pull-request:
        name: Pull Request
        uses: NixySoftware/.github/.github/workflows/pull-request.yml@d2eab9162b8786a1cccdc33b4725f3045e119927 # v0.5.1
