name: Pull Request

on:
    pull_request:
        types: [opened, synchronize, reopened, edited]

permissions:
    contents: read

jobs:
    pull-request:
        name: Pull Request
        uses: NixySoftware/.github/.github/workflows/pull-request.yml@69872c1a394b92973a42797e1bd19441722101bc # v0.5.0
